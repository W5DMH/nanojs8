# ESP-IDF build output
build/
managed_components/

# Generated sdkconfig (different from the checked-in sdkconfig.defaults / .dev / .release)
sdkconfig
sdkconfig.old

# Component manager lockfile (regenerated on first build)
dependencies.lock

# IDE / editor
.vscode/
.cache/
.clangd/
compile_commands.json
*.swp
*~

# CI artifacts (only generated during release builds)
nanojs8-merged.bin
docs/nanojs8-merged.bin

# OS
.DS_Store
Thumbs.db
